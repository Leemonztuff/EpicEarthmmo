{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd game-server && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "node game-server/dist/index.js",
    "healthcheckPath": "/health",
    "restartPolicyType": "always"
  }
}
