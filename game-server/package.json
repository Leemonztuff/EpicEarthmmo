{
  "name": "epic-earth-mmo-server",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "tsx watch index.ts",
    "build": "esbuild index.ts --bundle --platform=node --packages=external --outdir=dist",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "express": "^5.2.1",
    "socket.io": "^4.8.3"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^20",
    "esbuild": "^0.28.0",
    "tsx": "^4.22.0",
    "typescript": "5.9.3"
  }
}
